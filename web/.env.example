# Get these 6 values from: Firebase Console → Project Settings → General →
# "Your apps" → Web app → SDK setup and configuration → Config.
# (These are safe to be public — they identify the project, they don't grant access.
# Access control is enforced by firestore.rules / storage.rules, not by hiding these.)

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=welfare-e840b.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=welfare-e840b
VITE_FIREBASE_STORAGE_BUCKET=welfare-e840b.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

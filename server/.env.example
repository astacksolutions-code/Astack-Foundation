# Save your ROTATED service account JSON (see README) as ./serviceAccountKey.json
# — this file is already gitignored, never commit it or paste it in chat.
PORT=4000
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@umeedfoundation.org
